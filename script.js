// Jika ingin menggunakan animasi ketik manual, pakai ini:
// (Opsional, karena CSS sudah cukup untuk efek ketik)

const text = "I'm a Web Developer";
const target = document.getElementById("typing-text");

target.innerHTML = text;